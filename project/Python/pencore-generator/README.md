# pencore 生成器

BpbjsGreen 在初二时写的项目。

用一张图片的不同亮度版本拼出另一张图片。

## 使用说明

请将图片转换为 `jpg` 格式。

假设要用 `input1.jpg` 的不同亮度版本拼出 `input2.jpg`。

1. 将 `input1.jpg`，`input2.jpg` 放在 `main.py` 的同级目录下。
2. 运行 `main.py`。
3. 程序会建立子文件夹 `img` 存放 `input1.jpg` 的不同亮度版本。
4. 生成结果为 `output.jpg`。

注意：`input1.jpg` 的长宽必须相等。